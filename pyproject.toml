# Día 9 — pyproject.toml
[project]
name = "aws-docs-rag-assistant"
version = "0.1.0"
dependencies = [
    "rank-bm25>=0.2.2",
    "ragas>=0.2.0",
    "anthropic>=0.40.0",
    "langchain-anthropic>=0.3.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
